# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Database / ORM / migrations ---
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0

# --- Pydantic / validation / settings ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.2,<3.0

# --- Auth / security ---
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
pyotp>=2.9,<3.0
qrcode>=7.4,<8.0

# --- File uploads ---
python-multipart>=0.0.9,<0.1.0

# --- AI (HTTP client for NVIDIA OpenAI-compatible API) ---
httpx>=0.27,<1.0

# --- PDF reports ---
reportlab>=4.2,<5.0

# --- Testing ---
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0  # ASGITransport for tests